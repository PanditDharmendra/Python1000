s = {8, 7, 12, "Harry", [1,2]}
# The above code will raise a TypeError because sets cannot contain mutable objects
# like lists. sets can only contain immutable objects like strings,numbers and tuples.

